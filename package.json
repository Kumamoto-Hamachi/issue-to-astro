{
  "name": "issue-to-astro",
  "version": "0.0.0",
  "private": true,
  "description": "GitHub Issue を close したときに Markdown ファイルを自動生成し PR を作成する GitHub Action",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "license": "MIT",
  "engines": {
    "node": ">=24.0.0"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/exec": "^3.0.0",
    "@actions/github": "^9.0.0"
  }
}
